open grid_2d

let get_parent_plane c =
  failwith "Unimplemented"

let accounted_for cl =
  failwith "Unimplemented"

let win_evaluation c p1 p2 p3 =
  let cl = get_plane_referred in
  if (three_row_2d c cl) && (accounted_for cl) then true
  else
    

let cells_occupied p1 p2 p3 =
  failwith "Unimplemented"
